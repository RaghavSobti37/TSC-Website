name: Send WhatsApp Reminders

# Deprecated — book-call reminders now run in Taskmaster CRM (Render).
# Kept for manual inspection only; scheduled cron disabled.

on:
  workflow_dispatch:

jobs:
  ping-reminder-api:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "Reminders moved to Taskmaster — do not re-enable without removing TSC Google Sheet dependency."
